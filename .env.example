# ─── Required ─────────────────────────────────────────────────────────────────

# Port the server listens on (Render sets this automatically)
PORT=3000

# Firebase Admin SDK service account — paste the full JSON as a single-line string.
# Get it from: Firebase Console → Project Settings → Service Accounts → Generate new private key
FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"vee-chat-36720",...}

# ─── Optional ─────────────────────────────────────────────────────────────────

# Firebase Realtime Database URL.
# Defaults to: https://vee-chat-36720-default-rtdb.asia-southeast1.firebasedatabase.app
# Only set this if you're using a different Firebase project or region.
# FIREBASE_DATABASE_URL=https://your-project-default-rtdb.firebaseio.com

# Log level: trace | debug | info | warn | error | fatal  (default: info)
# LOG_LEVEL=info

# ─── Pre-existing production env vars (already set on Render) ─────────────────
# These are used by other parts of the production backend and are listed here
# for reference only — this api-server package does not directly use them.
#
# ONESIGNAL_APP_ID=...
# ONESIGNAL_REST_API_KEY=...
# CLOUDINARY_CLOUD_NAME=...
# CLOUDINARY_API_KEY=...
# CLOUDINARY_API_SECRET=...
